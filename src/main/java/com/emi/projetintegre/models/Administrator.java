package com.emi.projetintegre.models;

import java.time.LocalDate;
//import java.util.List;

//import com.emi.projetintegre.services.UserService;

public class Administrator extends User {
    //private List<String> privileges;

    public void createClient(String login, String password, LocalDate validityDate) {
        //Client newClient = new Client(login, password, validityDate);
        //UserService userService = new UserService();
        //userService.registerUser(newClient);
    }
}
